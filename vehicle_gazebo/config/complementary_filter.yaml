imu_complementary_filter:
  ros__parameters:

    # Magnetómetro
    use_mag: true

    # Ganancia
    gain_acc: 0.1
    gain_mag: 0.1

    do_bias_estimation: false
    do_adaptive_gain: true

    publish_tf: false
    remove_gravity_vector: false
    fixed_frame: odom